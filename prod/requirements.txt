# Dependencias de la app de inferencia (prod/).
# Versiones FIJADAS a las probadas localmente (Python 3.11).
#
# TORCH / GPU vs CPU:
#   Streamlit Community Cloud NO tiene GPU, por eso acá se usa la build CPU de
#   torch/torchvision (extra-index-url de PyTorch CPU). La app detecta CPU/GPU
#   en runtime, así que este mismo archivo sirve también en una máquina con GPU
#   (solo correría en CPU). Para desarrollo local con GPU, usá la build CUDA
#   (cu128) en tu entorno; no la fijes acá para no romper el deploy.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.58.0
torch==2.11.0
torchvision==0.26.0
pillow==12.2.0
pandas==3.0.3
numpy==2.4.4
opencv-python-headless==4.13.0

# Descarga del modelo (~158 MB) desde Google Drive en el deploy. gdown maneja
# la pantalla de confirmación de antivirus que Drive muestra para archivos
# grandes, que urllib no sabe sortear. Ver MODEL_URL en prod/utils.py.
gdown==5.2.0
